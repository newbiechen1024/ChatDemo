<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.newbiechen.chatdemo.MainActivity">

    <com.newbiechen.chatdemo.widget.RefreshListView
        android:id="@+id/main_rlv"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@null"
        android:dividerHeight="3dp"
        android:focusable="false"
        android:clickable="false">
    </com.newbiechen.chatdemo.widget.RefreshListView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="4dp">

        <EditText
            android:id="@+id/main_et_content"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:maxLines="4"
            android:background="@color/replyFrame"/>

        <Button
            android:id="@+id/main_btn_send"
            android:layout_width="60dp"
            android:layout_height="40dp"
            android:layout_marginLeft="4dp"
            android:text="发送"
            android:textColor="@android:color/white"/>
    </LinearLayout>
</LinearLayout>
